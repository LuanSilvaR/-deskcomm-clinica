import type { Icon as PhosphorIcon } from "@phosphor-icons/react";

import {
  CalendarBlank,
  ChatsCircle,
  CurrencyCircleDollar,
  FileText,
  Gear,
  House,
  IdentificationCard,
  ListChecks,
  Megaphone,
  Percent,
  Receipt,
  Robot,
  ShieldCheck,
  Sparkle,
  UserCircle,
  UsersThree,
  Wrench,
} from "@/lib/ui/icons";

import type { IconeDoModulo } from "./modulos";

/** FORK clinic (9014): o ícone de cada módulo do menu da clínica. */
export const ICONES_DOS_MODULOS: Record<IconeDoModulo, PhosphorIcon> = {
  CalendarBlank,
  ChatsCircle,
  CurrencyCircleDollar,
  FileText,
  Gear,
  House,
  IdentificationCard,
  ListChecks,
  Megaphone,
  Percent,
  Receipt,
  Robot,
  ShieldCheck,
  Sparkle,
  UserCircle,
  UsersThree,
  Wrench,
};
